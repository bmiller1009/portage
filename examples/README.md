# examples

Example portable workload definitions, used by tests, the CLI, and the Phase 0 exit demonstration. `wordcount.yaml` is the minimal fixture used by `tests/unit/test_workload_schema.py` and `plane workload validate`.
