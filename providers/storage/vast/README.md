# providers/storage/vast

VAST storage provider. Supports two distinct, explicitly-configured access modes — VAST NFS (via Kubernetes CSI) and VAST S3 — and does not treat them as equivalent internally (`docs/architecture/spec.md` §48).

Not yet implemented — v0.2 milestone.
