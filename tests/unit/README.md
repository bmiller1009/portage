# tests/unit

Fast, isolated tests with no external infrastructure: workload parsing, schema validation, dataset resolution, environment resolution, capability matching, state transitions, provider mappings, RBAC, configuration merging, retry decisions (`docs/architecture/spec.md` §52.1).
