# charts

Helm charts for deploying the control plane, reconciler, and their PostgreSQL dependency to Kubernetes (`docs/architecture/spec.md` §46). The control plane itself remains container-portable.

Not yet implemented — v0.1 milestone (needed for the `make dev` local stack).
